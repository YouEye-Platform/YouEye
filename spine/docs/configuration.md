# Spine Configuration Guide

This document describes the configuration system for YE-Spine.

## Overview

Spine uses a YAML configuration file to customize behavior. Configuration follows this precedence (highest to lowest):

1. **CLI flags** - `--config /path/to/config.yaml`
2. **Environment variables** - `SPINE_*` prefix
3. **Config file** - `/etc/spine/config.yaml`
4. **Defaults** - Built-in sensible defaults

Spine works without a config file - all values have sensible defaults.

## Quick Start

Generate a default configuration file:

```bash
spine config init
```

This creates `/etc/spine/config.yaml` with all defaults and comments.

View current configuration:

```bash
spine config show
```

Validate configuration:

```bash
spine config validate
```

## Configuration File Location

Default: `/etc/spine/config.yaml`

Override via CLI:

```bash
spine --config /custom/path/config.yaml deploy
```

## Configuration Options

### Releases

Configure where Spine fetches updates from:

```yaml
releases:
  provider: "github"                  # "github" (default) or "gitea"
  base_url: "https://github.com"      # Base URL for release downloads
  organization: "YouEye-Platform"     # Organization/owner name
  repositories:
    spine: "YouEye"                   # Repository for Spine releases
    control_panel: "YouEye"           # Repository for Control Panel releases
    ui: "YouEye"                      # Repository for UI releases
```

### Deployment

Configure container and application settings:

```yaml
deployment:
  container:
    name: "youeye-control"       # Container name
    image: "images:debian/12"    # Base image for container
  control_panel:
    app_dir: "/opt/app"          # App directory inside container
    port: 3000                   # Port to expose
    service_name: "youeye-control" # systemd service name
```

### API

Configure the Spine API server:

```yaml
api:
  socket_path: "/var/run/youeye/youeye.sock"  # Unix socket path
  timeout: 30                                   # Request timeout in seconds
```

### Paths

Configure system paths:

```yaml
paths:
  spine_binary: "/usr/local/bin/youeye"  # YouEye binary location
  systemd_service: "/etc/systemd/system/youeye.service"
  socket_dir: "/var/run/youeye"
```

### Security

Security-related settings:

```yaml
security:
  rate_limit_requests: 5     # Max auth attempts
  rate_limit_window: 300     # Window in seconds (5 minutes)
  backup_on_update: true     # Create backup before self-update
```

### Logging

Configure logging behavior:

```yaml
logging:
  level: "info"    # debug, info, warn, error
  format: "text"   # text or json
```

## Environment Variables

All config options can be set via environment variables with `SPINE_` prefix:

```bash
export SPINE_RELEASES_BASE_URL="https://custom.git.server"
export SPINE_DEPLOYMENT_CONTAINER_NAME="my-container"
export SPINE_API_SOCKET_PATH="/custom/socket.sock"
export SPINE_LOGGING_LEVEL="debug"
```

Environment variables use underscore-separated paths matching the YAML structure.

## Complete Default Configuration

```yaml
# Spine Configuration
# Generated by: spine config init

# Release source configuration
releases:
  provider: "github"
  base_url: "https://github.com"
  organization: "YouEye-Platform"
  repositories:
    spine: "YouEye"
    control_panel: "YouEye"
    ui: "YouEye"

# Deployment configuration
deployment:
  container:
    name: "youeye-control"
    image: "images:debian/12"
  control_panel:
    app_dir: "/opt/app"
    port: 3000
    service_name: "youeye-control"

# API server configuration
api:
  socket_path: "/var/run/youeye/youeye.sock"
  timeout: 30

# System paths
paths:
  spine_binary: "/usr/local/bin/youeye"
  systemd_service: "/etc/systemd/system/youeye.service"
  socket_dir: "/var/run/youeye"

# Security settings
security:
  rate_limit_requests: 5
  rate_limit_window: 300
  backup_on_update: true

# Logging configuration
logging:
  level: "info"
  format: "text"
```

## Validation

Spine validates configuration on load. Invalid configurations will prevent startup and show an error message.

Common validation errors:

- Missing required URLs
- Invalid port numbers (must be 1-65535)
- Invalid paths
- Invalid log levels

## Migrating from Previous Versions

Spine 0.1.0 introduces the configuration system. Previous versions used hardcoded values which are now the defaults:

| Previous Hardcoded | Config Path | Default |
|-------------------|-------------|---------|
| `https://github.com` | `releases.base_url` | `https://github.com` |
| `youeye-control` | `deployment.container.name` | `youeye-control` |
| `/opt/app` | `deployment.control_panel.app_dir` | `/opt/app` |
| Port 3000 | `deployment.control_panel.port` | `3000` |

**No action required** - existing deployments will work unchanged.

## Troubleshooting

### Config not loading

Check config file exists and is readable:

```bash
ls -la /etc/spine/config.yaml
spine config validate
```

### Environment variables not working

Ensure proper naming:

```bash
# Correct
export SPINE_RELEASES_BASE_URL="..."

# Incorrect (won't work)
export SPINE_RELEASES_BASEURL="..."
export RELEASES_BASE_URL="..."
```

### Reset to defaults

Delete the config file and Spine will use defaults:

```bash
rm /etc/spine/config.yaml
spine status  # Works with defaults
```
